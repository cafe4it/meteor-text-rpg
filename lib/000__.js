MUD = {} //namespace

MUD.Filter = {
    validateMessage: function (message) {

    },
    chat: function (message) {
    }
}

MUD.Dispatch = {
    extractCommand : function(fromFilter){},
    extractValue : function(fromFilter){}
}